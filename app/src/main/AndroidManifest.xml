<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Root tag for AndroidManifest.xml defining essential attributes about the app to Android system -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChronoMetron"
        tools:targetApi="31">

        <!-- LoginActivity which should be the first screen on app launch -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/login_title"
            android:theme="@style/Theme.ChronoMetron.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity which is the primary activity but not the first to be shown -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.ChronoMetron"
            android:exported="true">
        </activity>

        <!-- SignupActivity for new user registration -->
        <activity
            android:name=".SignupActivity"
            android:exported="true"
            android:label="@string/signup_title"
        android:theme="@style/Theme.ChronoMetron.NoActionBar">
    </activity>

</application>

    </manifest>
